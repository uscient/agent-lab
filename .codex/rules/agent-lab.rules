# >>> GENERATED by tools/render-adapters.sh — DO NOT EDIT (edit policy/* and re-run) >>>
# Codex execpolicy: allow the scoped AGENTS.md workflow. The PreToolUse guard
# (tools/pretooluse-guard.sh) is the AUTHORITATIVE denier; these are belt-and-suspenders.
# Branch-aware push/rebase/PR-create decisions are guard-enforced, not expressible here.

# -- allow: local git (clean token prefixes only; workspace-write auto-runs the rest) --
prefix_rule(pattern = ["shellcheck"], decision = "allow", justification = "agent-lab lint")
prefix_rule(pattern = ["git", "add"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "commit"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "fetch"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "switch"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "checkout"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "branch"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "stash"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "restore"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "status"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "diff"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "log"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "show"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "merge"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "rebase"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["git", "push"], decision = "allow", justification = "agent-lab local-git set")
prefix_rule(pattern = ["gh", "pr"], decision = "allow", justification = "agent-lab scoped GitHub workflow")
prefix_rule(pattern = ["gh", "run"], decision = "allow", justification = "agent-lab scoped GitHub workflow")

# -- forbidden: authentication and remote mutations outside the scoped workflow --
prefix_rule(pattern = ["git", "pull"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["git", "config"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["git", "credential"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "auth"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "api"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "repo"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "issue"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "release"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "workflow"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "run"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "secret"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "variable"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "project"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "gist"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "cache"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "codespace"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "extension"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "alias"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "ssh-key"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "gpg-key"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "label"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "pr", "merge"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "pr", "close"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "pr", "edit"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "pr", "ready"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "pr", "reopen"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "pr", "review"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "pr", "comment"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "release", "create"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "release", "delete"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "release", "edit"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["gh", "release", "upload"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["git", "remote", "add"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["git", "remote", "set-url"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["git", "remote", "remove"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
prefix_rule(pattern = ["git", "remote", "rename"], decision = "forbidden", justification = "agent-lab: outside the AGENTS.md scoped workflow")
# <<< END GENERATED <<<
