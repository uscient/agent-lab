name: CI
run-name: CI / ${{ github.event.pull_request.number || github.ref_name }}

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [dev, flow, master, main]
  pull_request:
    types: [opened, synchronize, reopened, edited, ready_for_review]
    branches: [dev, flow, master, main, 'work/**', 'group/**']
  merge_group:
    types: [checks_requested]

permissions:
  contents: read

jobs:
  fast:
    name: Fast
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      diff-base: ${{ steps.diff-base.outputs.base }}
      tested-head: ${{ steps.diff-base.outputs.head }}
      classification: ${{ steps.fast-gate.outputs.classification }}
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          fetch-depth: 0

      - name: Install Fast gate host tools
        run: sudo apt-get update -qq && sudo apt-get install -y -qq bubblewrap shellcheck

      - name: Validate pull request topology
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
          PR_HEAD_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name }}
          REPOSITORY: ${{ github.repository }}
        run: |
          if [ "$EVENT_NAME" != pull_request ]; then
            exit 0
          fi
          if [ "$PR_HEAD_REPOSITORY" != "$REPOSITORY" ]; then
            printf '::error title=Invalid PR repository::cross-repository PRs are not accepted\n'
            exit 1
          fi

          component='[a-z0-9][a-z0-9-]{0,47}'
          group='[gb][0-9]+[a-z]?-[a-z0-9][a-z0-9-]*'
          valid_group() {
            [ "${#1}" -le 48 ] && [[ "$1" =~ ^$group$ ]]
          }
          case "$PR_BASE_REF" in
            flow)
              group_name="${PR_HEAD_REF#group/}"
              [ "$PR_HEAD_REF" = "group/$group_name" ] && valid_group "$group_name"
              ;;
            group/*)
              group_name="${PR_BASE_REF#group/}"
              valid_group "$group_name" &&
                [[ "$PR_HEAD_REF" =~ ^slice/group/$group_name/$component$ ]]
              ;;
            work/*)
              work_name="${PR_BASE_REF#work/}"
              [[ "$work_name" =~ ^$component$ ]] &&
                [[ "$PR_HEAD_REF" =~ ^slice/$work_name/$component$ ]]
              ;;
            dev)
              [[ ! "$PR_HEAD_REF" =~ ^(group|slice)/ ]]
              ;;
            master | main)
              [[ ! "$PR_HEAD_REF" =~ ^(group|slice)/ ]]
              ;;
            *) false ;;
          esac || {
            printf '::error title=Invalid PR topology::%s cannot target %s\n' \
              "$PR_HEAD_REF" "$PR_BASE_REF"
            exit 1
          }

      - name: Validate pull request evidence
        if: ${{ github.event_name == 'pull_request' }}
        env:
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PR_BASE_REF: ${{ github.event.pull_request.base.ref }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          evidence_state="$(mktemp -d)"
          trap 'find "$evidence_state" -depth -delete >/dev/null 2>&1 || true' EXIT
          body="$evidence_state/body"
          old_body="$evidence_state/old-body"
          if ! python3 - "$GITHUB_EVENT_PATH" "$body" "$old_body" <<'PY'
          import json
          import pathlib
          import sys

          event_path = pathlib.Path(sys.argv[1])
          body_path = pathlib.Path(sys.argv[2])
          old_body_path = pathlib.Path(sys.argv[3])
          try:
              payload = json.loads(event_path.read_text(encoding="utf-8"))
              body = payload["pull_request"]["body"]
              if not isinstance(body, str):
                  raise TypeError("pull request body is not text")
              body_path.write_text(body, encoding="utf-8")
              prior_body = payload.get("changes", {}).get("body", {}).get("from")
              if prior_body is not None:
                  if not isinstance(prior_body, str):
                      raise TypeError("prior pull request body is not text")
                  old_body_path.write_text(prior_body, encoding="utf-8")
          except (OSError, KeyError, TypeError, json.JSONDecodeError) as error:
              print(f"INFRA cannot read pull request evidence: {error}", file=sys.stderr)
              raise SystemExit(125)
          PY
          then
            exit 125
          fi

          if [ -f "$old_body" ]; then
            ./scripts/dev/workflow-check evidence-append "$old_body" "$body"
          fi

          evidence_mode=pr-body
          case "$PR_BASE_REF" in
            flow | group/* | work/*) evidence_mode=pr-body-strict ;;
          esac
          case "$PR_HEAD_REF" in
            flow | group/* | slice/group/*) evidence_mode=pr-body-strict ;;
          esac

          if [ "$evidence_mode" != pr-body-strict ]; then
            policy_file="$evidence_state/protected.paths"
            changed_file="$evidence_state/changed.paths"
            if ! GIT_NO_REPLACE_OBJECTS=1 git show \
              "$PR_BASE_SHA:policy/protected.paths" > "$policy_file" ||
               ! GIT_NO_REPLACE_OBJECTS=1 git diff --name-only -z \
              "$PR_BASE_SHA" "$PR_HEAD_SHA" -- > "$changed_file"; then
              printf '::error title=Evidence applicability unknown::cannot classify changed paths\n'
              exit 125
            fi
            is_protected_path() {
              local changed="$1" entry
              while IFS= read -r entry; do
                case "$entry" in '' | \#*) continue ;; esac
                case "$entry" in
                  */) [[ "$changed" == "$entry"* ]] && return 0 ;;
                  *) [ "$changed" = "$entry" ] && return 0 ;;
                esac
              done < "$policy_file"
              return 1
            }
            while IFS= read -r -d '' changed; do
              if is_protected_path "$changed"; then
                evidence_mode=pr-body-strict
                break
              fi
            done < "$changed_file"
          fi

          ./scripts/dev/workflow-check "$evidence_mode" "$body" \
            "$PR_BASE_SHA" "$PR_HEAD_SHA" "$PR_HEAD_REF" "$PR_BASE_REF"

      - name: Resolve committed diff base
        id: diff-base
        env:
          EVENT_NAME: ${{ github.event_name }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
          PUSH_AFTER_SHA: ${{ github.event.after }}
          REF_NAME: ${{ github.ref_name }}
          MERGE_GROUP_BASE_SHA: ${{ github.event.merge_group.base_sha }}
          EXPECTED_HEAD_SHA: ${{ github.sha }}
          FLOW_R0_SHA: 9ef827c1b0c947babd90ed251deefcd50c04947c
        run: |
          sha_pattern='^([0-9a-f]{40}|[0-9a-f]{64})$'
          head="$(git rev-parse HEAD)"
          if [[ ! "$EXPECTED_HEAD_SHA" =~ $sha_pattern ]] ||
             [[ "$EXPECTED_HEAD_SHA" =~ ^0+$ ]] ||
             [ "$head" != "$EXPECTED_HEAD_SHA" ]; then
            printf '::error title=Unexpected tested head::expected %s, checked out %s\n' \
              "${EXPECTED_HEAD_SHA:-<empty>}" "${head:-<empty>}"
            exit 125
          fi

          case "$EVENT_NAME" in
            pull_request) base="$PR_BASE_SHA" ;;
            push)
              if [[ "$PUSH_BEFORE_SHA" =~ ^0+$ ]]; then
                if ! git fetch --no-tags origin \
                  '+refs/heads/dev:refs/remotes/origin/dev'; then
                  printf '::error title=Missing bootstrap authority::cannot resolve origin/dev\n'
                  exit 125
                fi
                dev_head="$(GIT_NO_REPLACE_OBJECTS=1 git rev-parse --verify 'origin/dev^{commit}')"
                if [[ ! "$FLOW_R0_SHA" =~ $sha_pattern ]] ||
                   [[ "$FLOW_R0_SHA" =~ ^0+$ ]] ||
                   ! GIT_NO_REPLACE_OBJECTS=1 git rev-parse --verify \
                     "${FLOW_R0_SHA}^{commit}" >/dev/null; then
                  printf '::error title=Missing bootstrap authority::recorded R0 commit is unavailable\n'
                  exit 125
                fi
                if ! GIT_NO_REPLACE_OBJECTS=1 git merge-base --is-ancestor "$FLOW_R0_SHA" "$dev_head"; then
                  printf '::error title=Invalid bootstrap authority::current origin/dev does not contain R0\n'
                  exit 125
                fi
                if [ "$REF_NAME" = flow ] && [ "$PUSH_AFTER_SHA" = "$head" ] &&
                   [ "$head" = "$dev_head" ]; then
                  base="$head"
                else
                  printf '::error title=Invalid branch creation::flow bootstrap closure must equal current origin/dev\n'
                  exit 125
                fi
              else
                base="$PUSH_BEFORE_SHA"
              fi
              ;;
            merge_group) base="$MERGE_GROUP_BASE_SHA" ;;
            *)
              printf '::error title=Unsupported CI event::%s has no deterministic diff base\n' \
                "$EVENT_NAME"
              exit 125
              ;;
          esac

          if [[ ! "$base" =~ $sha_pattern ]] || [[ "$base" =~ ^0+$ ]]; then
            printf '::error title=Invalid CI diff base::event supplied %s\n' \
              "${base:-<empty>}"
            exit 125
          fi
          if ! git rev-parse --verify "${base}^{commit}" >/dev/null; then
            printf '::error title=Unresolvable CI diff base::%s is not a fetched commit\n' \
              "$base"
            exit 125
          fi
          if ! git merge-base --is-ancestor "$base" HEAD; then
            printf '::error title=Invalid CI history::%s is not an ancestor of HEAD\n' \
              "$base"
            exit 125
          fi

          printf 'base=%s\n' "$base" >> "$GITHUB_OUTPUT"
          printf 'head=%s\n' "$head" >> "$GITHUB_OUTPUT"

      - name: Fast security gate
        id: fast-gate
        env:
          AGENT_LAB_DIFF_BASE: ${{ steps.diff-base.outputs.base }}
          CI_LOG_DIR: ${{ runner.temp }}/agent-lab-ci
        run: |
          set +e
          set -o pipefail
          mkdir -p "$CI_LOG_DIR"
          ./scripts/dev/ci-fast 2>&1 | tee "$CI_LOG_DIR/fast.log"
          rc=$?
          case "$rc" in
            0) classification=success ;;
            1) classification=assertion-failure ;;
            125) classification=infrastructure ;;
            *) classification=unexpected ;;
          esac
          printf 'classification=%s\n' "$classification" >> "$GITHUB_OUTPUT"
          exit "$rc"

      - name: Summarize fast gate
        if: ${{ always() }}
        env:
          RESULT: ${{ job.status }}
          DIFF_BASE: ${{ steps.diff-base.outputs.base || 'unresolved' }}
          HEAD_SHA: ${{ steps.diff-base.outputs.head || github.sha }}
        run: |
          {
            printf '## Fast gate\n\n'
            printf '| Result | Diff base | Tested head |\n'
            printf '|---|---|---|\n'
            printf '| `%s` | `%s` | `%s` |\n\n' "$RESULT" "$DIFF_BASE" "$HEAD_SHA"
            printf 'Replay: `AGENT_LAB_DIFF_BASE=%s ./scripts/dev/ci-fast`\n' \
              "$DIFF_BASE"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Retain fast failure log
        if: ${{ failure() || cancelled() }}
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: ci-fast-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/agent-lab-ci/fast.log
          if-no-files-found: ignore
          retention-days: 7

  static:
    name: Static
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      tested-head: ${{ steps.identity.outputs.head }}
      classification: ${{ steps.static-gate.outputs.classification }}
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Bind tested checkout
        id: identity
        env:
          EXPECTED_HEAD_SHA: ${{ github.sha }}
        run: |
          head="$(git rev-parse HEAD)"
          if [ "$head" != "$EXPECTED_HEAD_SHA" ]; then
            printf '::error title=Unexpected tested head::expected %s, checked out %s\n' \
              "$EXPECTED_HEAD_SHA" "$head"
            exit 125
          fi
          printf 'head=%s\n' "$head" >> "$GITHUB_OUTPUT"

      - name: Static configuration gate
        id: static-gate
        env:
          CI_LOG_DIR: ${{ runner.temp }}/agent-lab-ci
        run: |
          set +e
          set -o pipefail
          mkdir -p "$CI_LOG_DIR"
          ./tools/validate.sh --strict 2>&1 | tee "$CI_LOG_DIR/static.log"
          rc=$?
          case "$rc" in
            0) classification=success ;;
            1) classification=assertion-failure ;;
            125) classification=infrastructure ;;
            *) classification=unexpected ;;
          esac
          printf 'classification=%s\n' "$classification" >> "$GITHUB_OUTPUT"
          exit "$rc"

      - name: Summarize static gate
        if: ${{ always() }}
        env:
          RESULT: ${{ job.status }}
        run: |
          {
            printf '## Static gate\n\n'
            printf -- '- Result: `%s`\n' "$RESULT"
            printf -- '- Replay: `./tools/validate.sh --strict`\n'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Retain static failure log
        if: ${{ failure() || cancelled() }}
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: ci-static-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/agent-lab-ci/static.log
          if-no-files-found: ignore
          retention-days: 7

  docker:
    name: Docker security
    runs-on: ubuntu-latest
    timeout-minutes: 45
    outputs:
      tested-head: ${{ steps.identity.outputs.head }}
      classification: ${{ steps.docker-gate.outputs.classification }}
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Bind tested checkout
        id: identity
        env:
          EXPECTED_HEAD_SHA: ${{ github.sha }}
        run: |
          head="$(git rev-parse HEAD)"
          if [ "$head" != "$EXPECTED_HEAD_SHA" ]; then
            printf '::error title=Unexpected tested head::expected %s, checked out %s\n' \
              "$EXPECTED_HEAD_SHA" "$head"
            exit 125
          fi
          printf 'head=%s\n' "$head" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      - name: Build cache-aware devbox
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: .
          file: images/devbox/Dockerfile
          load: true
          tags: agent-lab/devbox:local
          cache-from: type=gha,scope=devbox
          cache-to: type=gha,mode=max,scope=devbox

      - name: Run Docker security gate
        id: docker-gate
        env:
          AGENT_LAB_DEVBOX_PREBUILT: 1
          CI_LOG_DIR: ${{ runner.temp }}/agent-lab-ci
        run: |
          set +e
          set -o pipefail
          mkdir -p "$CI_LOG_DIR"
          ./scripts/dev/docker-gate 2>&1 | tee "$CI_LOG_DIR/docker.log"
          rc=$?
          case "$rc" in
            0) classification=success ;;
            1) classification=assertion-failure ;;
            125) classification=infrastructure ;;
            *) classification=unexpected ;;
          esac
          printf 'classification=%s\n' "$classification" >> "$GITHUB_OUTPUT"
          exit "$rc"

      - name: Summarize Docker gate
        if: ${{ always() }}
        env:
          RESULT: ${{ job.status }}
        run: |
          {
            printf '## Docker security gate\n\n'
            printf -- '- Result: `%s`\n' "$RESULT"
            printf -- '- Replay: `./scripts/dev/docker-gate`\n'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Retain Docker failure log
        if: ${{ failure() || cancelled() }}
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: ci-docker-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/agent-lab-ci/docker.log
          if-no-files-found: ignore
          retention-days: 7

  required-gates:
    name: Required gates
    if: ${{ always() }}
    needs: [fast, static, docker]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5

      - name: Reduce required gate results
        env:
          CI_NEEDS_JSON: ${{ toJSON(needs) }}
          CI_EXPECTED_HEAD: ${{ github.sha }}
        run: ./scripts/dev/required-gates
