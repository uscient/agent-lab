#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." >/dev/null 2>&1 && pwd)"
cd "$repo_root"

if [ "$#" -ne 0 ]; then
  printf 'Usage: scripts/dev/ci-fast\n' >&2
  exit 2
fi

./scripts/dev/cue-tool provision
exec ./scripts/dev/check default quick
