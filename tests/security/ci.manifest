# Versioned required CI gates.
# Format:
#   version <schema-version>
#   gate <needs-key> <local replay command...>

version 1
gate fast ./scripts/dev/ci-fast
gate static ./tools/validate.sh --strict
gate docker ./scripts/dev/docker-gate
